% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/postgresql_variant_discovery.R
\name{pg_get_sample_metadata}
\alias{pg_get_sample_metadata}
\title{Retrieve sample metadata from the PostgreSQL database}
\usage{
pg_get_sample_metadata(con, query_col = NULL, query_value = NULL)
}
\arguments{
\item{con}{A \code{DBIConnection} object (PostgreSQL).}

\item{query_col}{Character. The metadata column to filter by (e.g., "countryorigin").
If \code{NULL}, all records are returned.}

\item{query_value}{Character or Numeric. The specific value to match in \code{query_col}.}
}
\value{
A data frame containing the sample metadata records.
}
\description{
This function fetches accession-level metadata, such as origin, race,
and classification, from the 'metadata' table. It supports optional
filtering by specific columns to subset populations for analysis.
}
\examples{
\dontrun{
library(panGenomeBreedr)
library(DBI)

con <- dbConnect(RPostgres::Postgres(), dbname = "sorghum_pangenome_db")

# Fetch metadata for all accessions originating from Ghana
ghana_samples <- pg_get_sample_metadata(con,
                                        query_col = "countryorigin",
                                        query_value = "Ghana")

dbDisconnect(con)
}

}
