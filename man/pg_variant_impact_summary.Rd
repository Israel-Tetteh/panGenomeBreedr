% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/postgresql_variant_discovery.R
\name{pg_variant_impact_summary}
\alias{pg_variant_impact_summary}
\title{Get variant impact summary from the PostgreSQL database}
\usage{
pg_variant_impact_summary(con)
}
\arguments{
\item{con}{A \code{DBIConnection} object, as returned by \code{\link[DBI]{dbConnect}}.}
}
\value{
A data frame in wide format where each row is a chromosome and
columns represent the counts for each impact category (e.g., \code{impact_HIGH}).
}
\description{
This function joins the variants and annotations tables to summarize the
distribution of mutation impacts (e.g., HIGH, MODERATE, LOW, MODIFIER)
across chromosomes.
}
\examples{
\dontrun{
library(panGenomeBreedr)
library(DBI)

con <- dbConnect(RPostgres::Postgres(),
                 dbname = "sorghum_pangenome_db",
                 host = "localhost",
                 user = "israeltawiahtetteh")

# Generate impact summary
impact_stats <- pg_variant_impact_summary(con)
head(impact_stats)

dbDisconnect(con)
}

}
